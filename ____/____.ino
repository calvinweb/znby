
void setup(){
pinMode(2,INPUT);
pinMode(3,OUTPUT);
pinMode(4,OUTPUT);

}
void pwm(int pin, int pos ) {
  digitalWrite(pin, 1);
  delayMicroseconds(pos);
  digitalWrite(pin, 0);
  delay(20);

}
void to(p) {
case 1:

}
void bph(){

}
void up(int pin) {
  pwm(pin , 1700);
}
void dowm(int pin) {
  pwm(pin, 1300)
}
void swi(byte bin) {
  switch (bin) {
    case 6:
      up
    case 15:

  }
}
